<%= render partial: 'pages/header', locals: { header: 'Editing Note', delete: true, resource: @note} %>

<%= render 'form', note: @note %>

<%= render partial: 'layouts/navigation', locals: { current: 'Home' } %>
